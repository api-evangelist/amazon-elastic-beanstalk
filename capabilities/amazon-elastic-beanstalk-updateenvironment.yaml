naftiko: 1.0.0-alpha2
info:
  label: 'Amazon Elastic Beanstalk AWS Elastic Beanstalk API — #UpdateEnvironment'
  description: 'Amazon Elastic Beanstalk AWS Elastic Beanstalk API — #UpdateEnvironment. 1 operations. Lead operation: Amazon
    Elastic Beanstalk Update Environment. Self-contained Naftiko capability covering one Amazon Elastic Beanstalk business
    surface.'
  tags:
  - Amazon Elastic Beanstalk
  - '#UpdateEnvironment'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_ELASTIC_BEANSTALK_API_KEY: AMAZON_ELASTIC_BEANSTALK_API_KEY
capability:
  consumes:
  - type: http
    namespace: amazon-elastic-beanstalk-updateenvironment
    baseUri: https://elasticbeanstalk.amazonaws.com
    description: 'Amazon Elastic Beanstalk AWS Elastic Beanstalk API — #UpdateEnvironment business capability. Self-contained,
      no shared references.'
    resources:
    - name: '#UpdateEnvironment'
      path: /#UpdateEnvironment
      operations:
      - name: updateenvironment
        method: POST
        description: Amazon Elastic Beanstalk Update Environment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.AMAZON_ELASTIC_BEANSTALK_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: amazon-elastic-beanstalk-updateenvironment-rest
    port: 8080
    description: 'REST adapter for Amazon Elastic Beanstalk AWS Elastic Beanstalk API — #UpdateEnvironment. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/updateenvironment
      name: updateenvironment
      description: 'REST surface for #UpdateEnvironment.'
      operations:
      - method: POST
        name: updateenvironment
        description: Amazon Elastic Beanstalk Update Environment
        call: amazon-elastic-beanstalk-updateenvironment.updateenvironment
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: amazon-elastic-beanstalk-updateenvironment-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for Amazon Elastic Beanstalk AWS Elastic Beanstalk API — #UpdateEnvironment. One tool per consumed
      operation, routed inline through this capability''s consumes block.'
    tools:
    - name: amazon-elastic-beanstalk-update-environment
      description: Amazon Elastic Beanstalk Update Environment
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: amazon-elastic-beanstalk-updateenvironment.updateenvironment
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
